# Transform: Redraw

Use only when the user explicitly requests re-authoring rather than preserving the source pixels.

Rebuild unlocked surroundings from the source's semantic anchors, orientation, viewpoint, dominant axis, subject scale and spatial relations. Preserve existing human bodies, clothing, poses and contact unless the user specifically authorizes redesign. Apply the already resolved human branch exactly once.

The selected route may reinterpret materials and atmosphere, but must not replace the source with a generic location, copy a reference background/character, or discard the scene's identifiable geometry and emotional relationship.
